/**
 * Oculta la sección de planes en la landing, la página /precios y los enlaces
 * que apuntan a ellas. Se reactiva poniendo `true` cuando los planes estén listos.
 */
export const PRICING_VISIBLE = false;
